# Loaded by prisma.config.ts (via dotenv) and by the test/seed scripts.
# Matches the credentials in docker-compose.yml.
DATABASE_URL="postgresql://trial:trial@localhost:5432/trial_booking?schema=public"
